{
  "name": "backend",
  "version": "1.0.0",
  "description": "Blogapp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mkdir -p reports && mocha --reporter mocha-junit-reporter --reporter-options mochaFile=reports/TEST-results.xml --exit"
  },
  "author": "Srineel Reddy",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.17.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.7",
    "supertest": "^6.3.3",
    "mocha-junit-reporter": "^2.0.0"
  }
}
